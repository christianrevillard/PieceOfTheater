<Page
    x:Class="PieceOfTheater.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PieceOfTheater"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBox Text="Testing main page" IsReadOnly="true"></TextBox>

        <TextBox Text="TODO - Text parse" IsReadOnly="true"></TextBox>
        <TextBox Text="Actes et scènes - liste + didascalie (feil title)" IsReadOnly="true"></TextBox>
        <TextBox Text="Répliques - stats + repliques par personnage" IsReadOnly="true"></TextBox>
        <TextBox Text="Scènes jouables" IsReadOnly="true"></TextBox>
        <TextBox Text="Tables de répétition" IsReadOnly="true"></TextBox>
        <TextBox TextWrapping="Wrap" Text="TextBox"/>
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="Input" Height="457" TextWrapping="Wrap" Text="{Binding Input}" Width="604"/>
            <TextBox x:Name="Ouput" Height="457" TextWrapping="Wrap" Text="{Binding Output}" Width="799"/>
        </StackPanel>
        <Button Content="Button" Height="152" Width="340" Command="{Binding Process}"/>
    </StackPanel>
</Page>
